# ATEM Tally System (M5Atom + Companion Custom Module) マニュアル

本システムは、M5Atom (ESP32) を物理的な Tally ランプとし、Bitfocus Companion 経由で ATEM スイッチャーの PGM/PVW 状態（キーヤー等の総合 Tally 状態）を連動させるためのシステムです。

## 1. システム全体構成

*   **物理デバイス (M5Atom)**: 
    *   LED マトリクス (5x5) および外部接続 LED (最大5系統) を持ち、WiFi 経由でネットワークに参加します。
    *   UDP プロトコルで送られてくるタリー情報を受信して点灯状態を切り替えます。
*   **制御ハブ (Bitfocus Companion & カスタムモジュール)**:
    *   ATEM スイッチャーとダイレクトに通信し、映像の切り替え状態を監視します。
    *   カスタムモジュールはネイティブモジュールに依存しない完全な JavaScript 実装 (v1.2.1+) を採用し、Companion 内で軽量かつ安定して動作します。
    *   ネットワーク内の M5Atom を mDNS (マルチキャストDNS) で自動検出し、連動して UDP コマンドを送信します。

---

## 2. デバイス (M5Atom) 側の仕様と機能

### 2.1 LED の点灯ルール
*   **PGM (赤)**: 配信中の映像。内蔵の 25個の LED 全てが赤色に光り、外部 LED も設定した明るさで点灯します。
*   **PVW (緑)**: プレビュー映像。設定で ON にしている場合、内蔵の 25個の LED 全てが緑色に光ります（外部 LED は消灯）。
*   **待機中 (暗い白・番号表示)**:
    *   PGM にも PVW にも選ばれていない場合。
    *   M5Atom 自身の LED マトリクスに、Companion で設定された **ATEM のカメラ入力番号 (1〜9)** が暗めの白色でふんわりと表示されます。
    *   ※ M5Atom を上下逆さまに取り付けて使うことを想定し、**数字は 180度反転して（逆さまに）描画** される仕様になっています。

### 2.2 その他の状態表示 (起動時など)
*   **青点滅**: WiFi 接続の試行中。
*   **黄色**: AP (アクセスポイント) モードで起動中。
*   **緑 (一瞬)**: WiFi 接続に成功。
*   **赤 (一瞬)**: WiFi 接続に失敗し、AP モードへフォールバック。
*   **青色点滅 (高速)**: Companion から「Identify」ボタンを押された際のデバイス特定アクション。

### 2.3 デバイスの設定画面 (AP モード / Web UI)
M5Atom 自身は Web サーバーを内蔵しており、以下の設定をブラウザから変更可能です。

*   **アクセス方法**:
    *   初回起動、または本体ボタンを押しながら起動すると、`Tally-Setup` という名前の WiFi が飛びます。
    *   PC やスマホで接続後、ブラウザで `http://192.168.4.1` を開くと設定画面が表示されます。
    *   （運用中は同じ WiFi 内から `http://[Device Name].local/` でアクセス可能です）
*   **設定項目**:
    *   **Device Name**: mDNS の検出名称（`cam1.local` など）。Companion のドロップダウンに表示される名前です。
    *   **SSID / Password**: 接続先の WiFi ルーター情報。
    *   **Brightness**: 外部 LED の明るさ (0〜255)。
    *   **Preview LED**: PVW 時の緑点灯を行うかどうかのトグルスイッチ（OFF の場合でも待機中と同様にカメラ番号が表示されます）。
    *   **OTA Firmware Update**: USB を繋がずにファームウェア (`.bin`) をワイヤレスで更新する機能。

---

## 3. Companion カスタムモジュール側の仕様と機能 (v1.2.x)

モジュール名: **ATEM Tally Custom**

### 3.1 概要
ATEM Connection 等の外部重厚ライブラリを廃し、ATEM の `TlSr` (Tally by Source) パケットを直接解釈する軽量内製クライアントを搭載しています。これにより、アップストリームキー (USK) やダウンストリームキー (DSK) など、合成状態を含めた **スイッチャー本体と完全に一致するタリー情報** を取得可能です。

### 3.2 モジュールの設定 (Configuration)
Companion の Connections (Instances) タブから各種設定を行います。

*   **ATEM Switcher IP**: ATEM スイッチャーの IP アドレスを入力します。
*   **Tally Device (mDNS) 1〜8**:
    *   ネットワーク内で検出された M5Atom 本体 (Device Name) がドロップダウンリストに自動表示されます。
    *   「M5Atom を後から起動」しても OS の mDNS 機能を介して自動でリストが更新されます。
*   **ATEM Input Num**:
    *   その Tally デバイスに割り当てる、ATEM 上の物理カメラ番号 (Input 番号) を設定します。
    *   ※ ここで番号を変更すると、M5Atom の画面に表示される白文字のカメラ番号も即座に切り替わります。

### 3.3 アクション (Actions)
ボタンに以下の機能を割り当てることができます。

*   **Force Tally Update**: ATEM に対して現在の状態を再確認し、全 M5Atom に対して点灯情報を強制的に再送信します。
*   **Identify Device**: 選択した M5Atom を青色で10回高速点滅させ、「どのカメラがどのランプか」を物理的に確認します。
*   **Set Brightness**: 特定の M5Atom の外部 LED の明るさを、Companion のボタン経由で変更(Dimming)します。

---

## 4. UDP 通信プロトコル仕様 (開発者向け)

Companion モジュールと M5Atom 間は UDP (ポート: 8888) で以下の単純な文字列コマンドを用いて通信しています。

*   **`pgm:[番号]`** : Program 点灯の指示。（例: `pgm:1`）
*   **`pvw:[番号]`** : Preview 点灯の指示。（例: `pvw:3`）
*   **`off:[番号]`** : 消灯（待機番号表示）の指示。（例: `off:2`）
    *   ※ [番号] の部分は Companion の「ATEM Input Num」で指定された数字が送られ、M5Atom はこの数字を画面に描画します。
*   **`dim:[値]`** : 明るさ変更。（例: `dim:128`）
*   **`identify`** : 特定用点滅アクションの実行。
