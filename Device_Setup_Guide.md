# Tally デバイス設定ガイド

このドキュメントでは、Tallyデバイス（M5Atom）自体のWiFi設定や動作設定を行うための「設定画面」へのアクセス方法を説明します。

## 1. 設定モード（APモード）での起動

初回起動時や、接続先WiFiを変更したい場合は、以下の手順で設定モードに入ります。

1.  **ボタンを押しながら起動**: M5Atom本体の正面ボタン（LED画面部分）を**押したまま**電源を入れます（またはリセットボタンを押します）。
2.  **LEDが黄色に点灯**: 本体LEDが**黄色**に点灯すれば、設定モード（アクセスポイントモード）で起動しています。
    - ※保存されているSSIDがない場合や、接続に失敗した場合も自動的にこのモードになります。

## 2. 設定画面へのアクセス

1.  **WiFiに接続**: PCやスマートフォンから、以下のWiFi（SSID）に接続します。
    - **SSID:** `Tally-Setup`
    - **Password:** (なし)
2.  **ブラウザで開く**: ブラウザ（Chrome/Safari等）を開き、アドレスバーに以下を入力します。
    - `http://192.168.4.1`

## 3. 設定項目の説明

設定画面では以下の項目を変更できます。

| 項目 | 説明 |
| :--- | :--- |
| **Device Name** | `tally-xxxxxx.local` という名前でアクセスできるようになります。Companion等の管理用IDとしても使われます。 |
| **SSID / Password** | 接続先のWiFiルーターの情報を入力します。 |
| **Brightness** | Tally点灯時の明るさ (0〜255) を設定します。 |
| **Preview LED** | Preview (緑) 時に本体内部のLEDを光らせるかどうかを選択します。 |

「**Save & Restart**」ボタンを押すと、設定が保存され、デバイスが再起動して指定したWiFiへの接続を試みます。

---

## 4. 運用中のアクセス (mDNS)

WiFi接続に成功している場合、同じネットワーク内のPCから以下のURLで設定画面を開き直すことができます。
- `http://[Device Name].local/`
- (例: `http://tally-a1b2c3.local/`)

※ Windows環境などで `.local` でアクセスできない場合は、シリアルモニタやルーターの管理画面で確認したIPアドレス（例: `http://192.168.1.50/`）を直接入力してください。

## 5. ファームウェアの更新 (OTA)

設定画面の下部には「**OTA Firmware Update**」セクションがあります。
新しい `.bin` ファイル（コンパイル済みのバイナリ）を選択してアップロードすることで、USBケーブルを繋がずにワイヤレスで機能を更新できます。
